name: workflow run

on: 
    push:
        branches: [main]

jobs: 
    build:
        uses: ./.github/workflows/reusable-build.yml
        with:
            app-name: "my-web-app"
            environment: "production"
        secrets:
            docker-token: ${{ secrets.DOCKER_TOKEN }}
            