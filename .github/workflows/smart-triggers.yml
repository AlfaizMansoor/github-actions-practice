name: smart triggers

on:
  push:
    branches: [main, release/*]
    paths:
      - 'src/**'
      - 'app/**'
    paths-ignore:
      - '*.md'
      - 'docs/**'

jobs: 
    smart-triggers:
        runs-on: ubuntu-latest
        steps: 
            - name: Triggers
              run: ls